<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="oasys_product.list">
      <field name="name">oasys_product list</field>
      <field name="model">oasys_product.oasys_product</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="oasys_product.action_window">
      <field name="name">oasys_product window</field>
      <field name="res_model">oasys_product.oasys_product</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="oasys_product.action_server">
      <field name="name">oasys_product server</field>
      <field name="model_id" ref="model_oasys_product_oasys_product"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="oasys_product" id="oasys_product.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="oasys_product.menu_1" parent="oasys_product.menu_root"/>
    <menuitem name="Menu 2" id="oasys_product.menu_2" parent="oasys_product.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="oasys_product.menu_1_list" parent="oasys_product.menu_1"
              action="oasys_product.action_window"/>
    <menuitem name="Server to list" id="oasys_product" parent="oasys_product.menu_2"
              action="oasys_product.action_server"/>
    -->
  </data>
</odoo>